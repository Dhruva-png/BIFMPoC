requests>=2.31.0
python-dotenv>=1.0.1
pymupdf>=1.24.0
openpyxl>=3.1.2
Pillow>=10.0.0
pytest>=8.0.0
streamlit>=1.38.0
pandas>=2.0.0
watchdog
# SharePoint (Microsoft Graph) intake/filing connector
# (auth only needs `requests`, already listed above)
# Email intake connector uses Python's built-in imaplib/email modules —
# no extra dependency needed.
# Gemini backend (app/llm/gemini_client.py): Google's own SDK is required
# here (not a plain `requests` call like the other backends) because
# Google's newer "auth key" API keys don't authenticate against the raw
# generateContent REST endpoint the same way the old standard keys did -
# the SDK is the only path confirmed to work with them. See that
# module's docstring.
google-genai>=2.13.0
